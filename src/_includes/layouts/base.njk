<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ description or site.tagline }}">
  <title>{% if title %}{{ title }} | {% endif %}{{ site.name }}</title>
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>

  <nav class="site-nav">
    <a href="/" class="site-logo">Wasatch <span>Wire</span></a>
    <ul class="nav-links">
      <li><a href="/" {% if navActive == 'home' %}class="active"{% endif %}>Home</a></li>
      <li><a href="/wire/" {% if navActive == 'wire' %}class="active"{% endif %}>Wire</a></li>
      <li><a href="/scores/" {% if navActive == 'scores' %}class="active"{% endif %}>Scores</a></li>
      <li><a href="/standings/" {% if navActive == 'standings' %}class="active"{% endif %}>Standings</a></li>
      <li><a href="/rankings/" {% if navActive == 'rankings' %}class="active"{% endif %}>Rankings</a></li>
      <li><a href="/players/" {% if navActive == 'players' %}class="active"{% endif %}>Players</a></li>
      <li><a href="/holy-war/" {% if navActive == 'holywar' %}class="active"{% endif %}>Holy War</a></li>
      <li><a href="/tabloids/" {% if navActive == 'tabloids' %}class="active"{% endif %}>Tabloids</a></li>
    </ul>
  </nav>

  {% if showTicker !== false %}
  <div class="ticker">
    <div class="ticker-inner">
      {% for item in ticker.items %}
        <div class="ticker-item"><span>{{ item }}</span></div>
        <div class="ticker-sep">·</div>
      {% endfor %}
      {% for item in ticker.items %}
        <div class="ticker-item"><span>{{ item }}</span></div>
        <div class="ticker-sep">·</div>
      {% endfor %}
    </div>
  </div>
  {% endif %}

  <main>
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <div class="footer-logo">Wasatch <span>Wire</span></div>
    <div>{{ site.name }} · Big 12 Dynasty · Season {{ site.season }}</div>
  </footer>

</body>
</html>
